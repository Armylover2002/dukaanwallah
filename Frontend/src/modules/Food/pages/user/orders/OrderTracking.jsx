/**
 * OrderTracking page — renders the user order details view.
 * Used at route: /food/user/orders/:orderId
 */
import UserOrderDetails from "./UserOrderDetails";

export default function OrderTracking() {
  return <UserOrderDetails />;
}
